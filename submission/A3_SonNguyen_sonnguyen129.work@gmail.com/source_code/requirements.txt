# Local CLI and grader dependencies
datasets>=2.18.0
huggingface-hub>=0.22.0
pydantic>=2.5.0
matplotlib>=3.8.0
numpy>=1.26.0
scipy>=1.12.0
tqdm>=4.66.0
python-dotenv>=1.0.0

# Cerebrium inference client (OpenAI-compatible HTTP + local tokenizer)
openai>=1.40.0
transformers>=4.51.0

# Optional: only needed for src/analysis.py diversity plots (pulls in torch).
# Local runs can pass --skip-analysis instead.
# sentence-transformers>=2.7.0
